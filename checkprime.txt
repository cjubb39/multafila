int checkPrime(int i) {
	int hello = 1;
}
int main (*string argv, int argc) {
	int count = 0;

	int threads[5];

	pfor(threads, i, 2) {
		int result = checkPrime(i);
		if (result < 5) {
			lock ( count ) {
				count = count + 1;
			}
		}

	}
}